{
  "version": 2,
  "buildCommand": "npm install && cd packages/ui && npm install && npm run build && cd ../../apps/web && npm install && npm run build",
  "installCommand": "npm install && cd packages/ui && npm install && cd ../../apps/web && npm install",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next",
  "regions": ["fra1"],
  "env": {
    "NEXTAUTH_URL": "https://all-service-hemma.vercel.app",
    "DEFAULT_LOCALE": "sv"
  }
} 